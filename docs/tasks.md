


#### 任务清单


- [✅] 系统设置-基础设置tab页签中移除「中继网络前缀」，后端使用该字段替换为「从配置文件中读取RELAY_NETWORK_PREFIX字段」
- [✅] 系统设置-基础设置tab页签中关于easytier-core版本管理的问题，只向上进行版本管理（只加载显示当前版本之后的版本），移除本地编译功能
- [✅] easytier-core本地内置默认基础版本，由脚本/workflow控制
- [ ~ ] 服务器化改造
- [ ] P2P 分布式服务器功能
- [✅] 空间分享功能（包括设置ip）实现加密/解密，扫码功能
- [✅] 多平台适配（Windows、Linux、iOS、Android）/ 支持docker化部署
- [✅] workflow打包机配置文件整理
- [ ] 多平台适配验证
- [ ~ ] 后端增加门户校验权限功能：（预留Referer防盗功能，现在直接放过）将用户第一次触发请求时先调用生成Cookie 接口，请求头中Host、Referer(可能为空)、user-agent组合生成唯一cookie。再次发起请求时开始校验cookie（静态资源「图片、html、css、javascript等」绕过校验），校验规则：先实现简单校验，判断是否在服务中生成过即可，暂不设置过期时间。

- [✅] 空间分析逻辑调整：点击分享按钮后，打开分享弹窗，此时不走定时任务加载创建二维码，应该等用户输入完成/不输入接收者ip，下方增加{文本提示「不提供IP时dhcp会默认为true」、按钮「创建分享」}，点击按钮后将ip（可能为空）传给后端，后端根据是否存在ip覆盖dhcp默认值（存在ip时dhcp = false， 不存在ip时，dhcp=true）创建加密分享链接、二维码。

- [ ] 分享加密压缩
- [ ] 空间内置应用「应用分配、应用分享（授权）；文件分享需要处理离网场景；」