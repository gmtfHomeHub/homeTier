# homeTier 前端构建配置模板
# 复制为 .env 后按需修改，`pnpm build` 时 Vite 自动读取。

# 前端静态资源公共前缀（默认 /；部署到 nginx 子路径/反向代理子目录时，
# 改为与 SERVER_PUBLIC_BASE（homeTier.conf）一致的前缀，如 /hometier/）。
# 注意：桌面/移动端构建必须保持 /（Tauri 以根路径加载 dist）。
VITE_PUBLIC_BASE=/