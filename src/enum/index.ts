export enum SpaceStatus {
  DIS = "disconnected",
  ING = "connecting",
  CED = "connected",
}

export enum SettingTabEnum {
  BASIC = "basic",
  LOG = "logs",
  ET = "easytier",
  CONFIG = "config",
}

export enum LanguageEnum {
  ZH = "zh",
  TW = "zh-TW",
  EN = "en"
}

export enum ThemeEnum {
  LIGHT = "light",
  DARK = "dark",
  SYS = "system",
}